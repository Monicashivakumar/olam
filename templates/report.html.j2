<!DOCTYPE html>
<html>
<head>
    <title>Oracle Linux Virtualization Manager Report</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f2f2f2;
        }
        a {
            text-decoration: none;
            color: #005ca6;
            cursor: pointer;
        }
        a:hover {
            color: #337ab7;
        }
        .section {
            margin: 20px;
            padding: 20px;
            background-color: #ffffff;
            border: 1px solid #ddd;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        .section h2 {
            margin-top: 0;
        }
        .logo {
            text-align: center;
            margin: 20px;
        }
        .logo img {
            width: 100px;
            height: auto;
        }
        .hidden {
            display: none;
        }
    </style>
</head>
<body>
    <div class="logo">
        <img src="/usr/share/oracle-logos/fedora_logo.svg" alt="Oracle Linux Virtualization Manager Logo">
    </div>

    <div class="section">
        <h2><a onclick="toggleSection('general-info')">1. General Info about the Environment</a></h2>
        <div id="general-info" class="hidden">
            <table>
                <tr>
                    <th>Category</th>
                    <th>Count</th>
                </tr>
                <tr>
                    <td>KVM Hosts</td>
                    <td>{{ hosts_info.ovirt_hosts | length }}</td>
                </tr>
                <tr>
                    <td>Hosts in Maintenance</td>
                    <td>{{ hosts_in_maintenance }}</td>
                </tr>
                <tr>
                    <td>Hosts Non Responsive</td>
                    <td>{{ hosts_non_responsive }}</td>
                </tr>
                <tr>
                    <td>Hosts Non Operational</td>
                    <td>{{ hosts_non_operational }}</td>
                </tr>
                <tr>
                    <td>Storage Domains</td>
                    <td>{{ storage_domains_info.ovirt_storage_domains | length }}</td>
                </tr>
                <tr>
                    <td>Clusters</td>
                    <td>{{ clusters_info.ovirt_clusters | length }}</td>
                </tr>
                <tr>
                    <td>Templates</td>
                    <td>{{ templates_info.ovirt_templates | length }}</td>
                </tr>
                <tr>
                    <td>Virtual Machines</td>
                    <td>{{ vms_info.ovirt_vms | length }}</td>
                </tr>
            </table>
        </div>
        <h2><a onclick="toggleSection('cluster-info')">2. Cluster Information</a></h2>
        <div id="cluster-info" class="hidden">
            <!-- Add cluster information here -->
        </div>
    </div>

    <script>
        function toggleSection(id) {
            var section = document.getElementById(id);
            if (section.classList.contains('hidden')) {
                section.classList.remove('hidden');
            } else {
                section.classList.add('hidden');
            }
        }
    </script>
</body>
</html>
