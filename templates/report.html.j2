<!DOCTYPE html>
<html>
<head>
    <title>Oracle Linux Virtualization Manager Report</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f2f2f2;
        }
        a {
            text-decoration: none;
            color: #005ca6;
        }
        a:hover {
            color: #337ab7;
        }
        .section {
            margin: 20px;
            padding: 20px;
            background-color: #ffffff;
            border: 1px solid #ddd;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        .section h2 {
            margin-top: 0;
        }
        .logo {
            text-align: center;
            margin: 20px;
        }
        .logo img {
            width: 100px;
            height: auto;
        }
    </style>
</head>
<body>
    <div class="logo">
        <img src="file:///usr/share/oracle-logos/fedora_logo.svg" alt="Oracle Linux Virtualization Manager Logo">
    </div>

    <h1>Oracle Linux Virtualization Manager Report</h1>

    <div class="section">
        <h2><a href="#general-info">General Info about the Environment</a></h2>
        <h2><a href="#cluster-info">Cluster Information</a></h2>
    </div>

    <div id="general-info" class="section">
        <h2>General Info about the Environment</h2>
        <table>
            <tr>
                <th>Category</th>
                <th>Count</th>
            </tr>
            <tr>
                <td>KVM Hosts</td>
                <td>{{ hosts_info.ovirt_hosts | length }}</td>
            </tr>
            <tr>
                <td>Hosts in Maintenance</td>
                <td>{{ hosts_in_maintenance }}</td>
            </tr>
            <tr>
                <td>Hosts Non Responsive</td>
                <td>{{ hosts_non_responsive }}</td>
            </tr>
            <tr>
                <td>Hosts Non Operational</td>
                <td>{{ hosts_non_operational }}</td>
            </tr>
            <tr>
                <td>Storage Domains</td>
                <td>{{ storage_domains_info.ovirt_storage_domains | length }}</td>
            </tr>
            <tr>
                <td>Clusters</td>
                <td>{{ clusters_info.ovirt_clusters | length }}</td>
            </tr>
            <tr>
                <td>Templates</td>
                <td>{{ templates_info.ovirt_templates | length }}</td>
            </tr>
            <tr>
                <td>Virtual Machines</td>
                <td>{{ vms_info.ovirt_vms | length }}</td>
            </tr>
        
        </table>
    </div>

    <div id="cluster-info" class="section">
        <h2>Cluster Information</h2>
        <!-- Add cluster information here -->
    </div>
</body>
</html>
